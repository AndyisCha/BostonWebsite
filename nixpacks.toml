[providers]
node = "18"

[variables]
NPM_CONFIG_PRODUCTION = "false"

[phases.install]
cmd = "cd server && npm ci"

[phases.build]
cmd = "cd server && npm run build"

[start]
cmd = "cd server && npm start"